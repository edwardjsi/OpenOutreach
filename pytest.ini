[pytest]
pythonpath = .
testpaths = tests
DJANGO_SETTINGS_MODULE = linkedin.django_settings
markers =
    no_embed_mock: skip the autouse fastembed mock
    allow_network: opt out of the autouse outbound-network block (never for live LinkedIn calls)
