The plan is complete and marked ready for approval, but I'm still in **plan mode** (read-only access) — I can't create files or write code until you approve the plan via the approval menu. The system transitions me out of read-only mode only after approval.

Once approved, here's exactly what I'll do first — **before touching any code** — and then implement the chunks in order:

**Decision document** → `.kimchi/docs/checkpoint-resolution-decision.md` (in the Documents directory, per guidelines), containing:

1. **Problem statement** — the re-authentication retry loop hammering LinkedIn on a checkpoint challenge (`daemon.py:337-344`), why it's dangerous (6 logins in ~10 min escalates checkpoints → restrictions), and the account-safety concern you raised.
2. **Solutions discussed** (with rationale for accept/reject):
   - Long auto-cooldown + keep running → rejected (still re-attempts, can hammer)
   - Stop the daemon entirely → rejected (no auto-recovery)
   - Short retry cooldown → rejected (re-hammers)
   - Generic webhook (ntfy/Slack/etc.) → superseded by your Telegram request
   - Email via SMTP → rejected (config burden higher than Telegram)
   - **Block + wait for manual VNC resolution + Telegram notification** → **finalized**
3. **Finalized solution** — detection in `goto_page`, indefinite block with heartbeat logging, Telegram push via Bot API + terminal bell fallback, `testtelegram` command, `CheckpointChallengeError` daemon guard.
4. **Setup steps** for Telegram (BotFather → @userinfobot → Admin fields → `make testtelegram`).
5. **Risks & mitigations** table.

Then I implement Chunks 1–5 in order, running `make test` and the migration checks as I go.

Approve the plan and I'll start with the document.

<done>